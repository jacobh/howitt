[package]
name = "rwgps"
version = "0.0.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
serde_path_to_error = "=0.1.20"
thiserror = "=2.0.20"
url = "=2.5.8"
rwgps_types = { path = "../rwgps-types" }
async-trait = "=0.1.92"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
reqwest = { version = "=0.13.5", default-features = false, features = [
    "json",
    "native-tls-vendored",
    "query",
] }
tokio = { version = "=1.53.1", features = ["full"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
# reqwest uses the browser/Workers Fetch API on wasm32 and must not enable a
# native TLS backend there.
reqwest = { version = "=0.13.5", default-features = false, features = ["json", "query"] }
tokio = { version = "=1.53.1", features = ["sync"] }
worker = { version = "=0.8.5", default-features = false }
