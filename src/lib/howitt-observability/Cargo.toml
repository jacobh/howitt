[package]
name = "howitt-observability"
version = "0.1.0"
edition = "2021"

[dependencies]
tracing = "0.1"

[target.'cfg(target_arch = "wasm32")'.dependencies]
worker = { version = "=0.8.5", default-features = false }
wasm-bindgen-futures = "0.4"
